---
title: bino setup
description: Install browser runtimes used for PDF rendering.
---

bino relies on a browser runtime to render PDFs.
Use `bino setup` to download and configure these dependencies.

## Usage

```bash
bino setup [flags]
```

Common flags:

- `--browser` – one or more browsers to install (for example `chromium`).
- `--driver-dir` – custom directory for the rendering driver and cache.
- `--dry-run` – show what would be installed without actually downloading.
- `--quiet` – reduce output noise.

## Examples

Install Chromium only:

```bash
bino setup --browser chromium
```

Install multiple browsers:

```bash
bino setup --browser chromium --browser firefox
```

If builds fail with rendering or browser errors, rerun this command and verify that your environment has network and disk access.

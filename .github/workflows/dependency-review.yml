# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Dependency Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  dependency-review:
    name: Dependency Review
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Harden the runner
        uses: step-security/harden-runner@ab7a9404c0f3da075243ca237b5fac12c98deaa5 # v2.19.3
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dependency Review
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          allow-licenses: MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, MPL-2.0, Unlicense, CC0-1.0, 0BSD
          allow-dependencies-licenses: >-
            pkg:golang/golang.org/x/crypto,
            pkg:golang/golang.org/x/mod,
            pkg:golang/golang.org/x/net,
            pkg:golang/golang.org/x/sync,
            pkg:golang/golang.org/x/sys,
            pkg:golang/golang.org/x/telemetry,
            pkg:golang/golang.org/x/term,
            pkg:golang/golang.org/x/text,
            pkg:golang/golang.org/x/tools
          comment-summary-in-pr: on-failure
